# Render Blueprint for Architecht
# One-click deploy: backend + frontend.
# Database is hosted externally on MongoDB Atlas (Free Tier).

services:
  # ----- Backend API (FastAPI) -----
  - type: web
    name: architecht-backend
    runtime: python
    plan: free                 # Changed to free tier
    region: singapore          # Closest Render free region to Sydney
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn server:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/
    autoDeploy: true
    envVars:
      # Paste your MongoDB Atlas cloud string directly in the Render dashboard
      - key: MONGO_URL
        sync: false             
      - key: DB_NAME
        value: architecht
      # Pre-wired to the default frontend hostname produced by this blueprint
      - key: CORS_ORIGINS
        value: https://onrender.com
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_ALGORITHM
        value: HS256
      - key: JWT_EXPIRY_DAYS
        value: "30"
      # ----- AWS Bedrock -----
      - key: AWS_BEARER_TOKEN_BEDROCK
        sync: false             # Paste your AWS Bedrock key in the dashboard
      - key: AWS_REGION
        value: ap-southeast-2   # Sydney
      - key: BEDROCK_MODEL_ID
        value: apac.anthropic.claude-sonnet-4-5-20250929-v1:0
      - key: PYTHON_VERSION
        value: "3.11.9"

  # ----- Frontend (static React build) -----
  - type: web
    name: architecht-frontend
    runtime: static            # Static sites are always free on Render
    rootDir: frontend
    buildCommand: corepack enable && yarn install --frozen-lockfile && yarn build
    staticPublishPath: build
    autoDeploy: true
    envVars:
      - key: REACT_APP_BACKEND_URL
        value: https://onrender.com
      - key: NODE_VERSION
        value: "20.11.0"
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    headers:
      - path: /*
        name: Cache-Control
        value: "no-cache, no-store, must-revalidate"
      - path: /static/*
        name: Cache-Control
        value: "public, max-age=31536000, immutable"
